#Sorting Arrays

#Sorting means putting elements in an ordered sequence.
#sort(), that will sort a specified array.

# Sort the array ?

import numpy as np

arr = np.array([3, 2, 0, 1])

print(np.sort(arr))

#Note: This method returns a copy of the array, l
# eaving the original array unchanged.